#!/usr/bin/env bash

set -Eeuo pipefail

echo "==> Registering qemu into /proc/sys/fs/binfmt_misc"
docker run --pull always \
           --privileged \
           --rm \
           docker.artifactory.hashicorp.engineering/prodsec-binfmt:latest --install all
